{
  "name": "layer-metadata-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p ."
  },
  "dependencies": {
    "apollo-server": "^3.11.1",
    "graphql": "^16.7.1",
    "graphql-subscriptions": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
